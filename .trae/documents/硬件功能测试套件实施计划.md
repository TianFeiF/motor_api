我已完成对现有代码库的调研，并为您设计了完整的硬件功能测试套件方案。

### 1. 测试文件结构设计
我们将创建 `test` 目录，并包含以下三个核心文件：

*   **`test/test_config.h`**
    *   **功能**: 集中管理测试配置，如 ENI 文件路径、预期硬件参数（VendorID=37, ProductCode=0x530）、测试周期（4ms）以及 ANSI 彩色日志宏。
    *   **硬件适配**: 直接映射 `HCFAX3E.xml` 中的从站信息。

*   **`test/test_motor_api.c`**
    *   **功能**: 实现具体的测试逻辑函数。
    *   **测试覆盖**:
        1.  **ENI 解析测试**: 验证能否正确读取 XML 中的厂商 ID 和产品码。
        2.  **硬件初始化测试**: 调用 `motor_api_create` 建立 EtherCAT 主站。
        3.  **运动控制测试**: 验证使能流程（CiA-402 状态机）、CSP 模式指令下发及状态反馈。
        4.  **HTTP 接口测试**: 启动/停止 HTTP 服务，验证端口占用与释放。
        5.  **性能基准测试**: 统计 `motor_api_run_once` 的平均耗时与抖动。
        6.  **错误处理测试**: 传入空指针、非法路径等边界条件，验证系统稳定性。

*   **`test/test_main.c`**
    *   **功能**: 测试程序的入口，负责参数解析与测试报告生成。
    *   **特性**: 支持命令行参数（如 `./hardware_test --skip-motion`）来灵活控制测试范围。

### 2. 构建系统更新
需要修改根目录下的 **`CMakeLists.txt`**，添加新的测试可执行目标 `hardware_test`，并链接到 `motor_api_static` 库。

### 3. 实施步骤
1.  **创建目录**: 建立 `test` 文件夹。
2.  **编写配置**: 创建 `test_config.h`，定义测试常量。
3.  **编写用例**: 创建 `test_motor_api.c`，实现上述 6 大测试模块。
4.  **编写主控**: 创建 `test_main.c`，串联所有测试。
5.  **更新构建**: 修改 `CMakeLists.txt`。
6.  **编译验证**: 运行 `cmake` 和 `make`，确保测试程序可生成。

该方案完全基于您提供的 XML 和头文件设计，确保代码与硬件实际参数一致。确认无误后，我将开始创建文件。